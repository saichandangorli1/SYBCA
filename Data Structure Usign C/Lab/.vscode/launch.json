{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "(MSVC) Launch", // Name of your debug configuration
            "type": "cppvsdbg",     // Debugger type for Visual Studio
            "request": "launch",
            "program": "${workspaceFolder}/a.exe", // Path to your compiled executable
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "externalTerminal", // or "integratedTerminal"
            "preLaunchTask": "C/C++: cl.exe build active file" // This task will build your code before debugging
        }
    ]
}